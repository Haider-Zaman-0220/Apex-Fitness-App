<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="40dp">

    <EditText
        android:id="@+id/planTitleEditText"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="Plan Title"
        android:inputType="text"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/planDescriptionEditText"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="Plan Description"
        android:inputType="textMultiLine"
        android:lines="3"
        android:layout_marginBottom="8dp" />

    <Spinner
        android:id="@+id/planTypeSpinner"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/assignToEditText"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="Assign to Member UID"
        android:inputType="text"
        android:layout_marginBottom="8dp" />

</LinearLayout>
